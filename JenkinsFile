pipeline {
    agent {label 'Agente1'}

    stages {
        stage('cloning') {
            steps {
                git branch: 'master', url: 'https://github.com/nekjoes/Mod8ProyFinal.git'
            }
        }
        stage ('build'){
            steps{
                bat "docker-compose ps"
            }
        }
        
    }
}
